{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Plat</th>
                <td>{{ commande.plat }}</td>
            </tr>
            <tr>
                <th>Dessert</th>
                <td>{{ commande.dessert }}</td>
            </tr>
            <tr>
                <th>Canette</th>
                <td>{{ commande.canette }}</td>
            </tr>
            <tr>
                <th>Eau</th>
                <td>{{ commande.eau }}</td>
            </tr>
            <tr>
                <th>Boisson</th>
                <td>{{ commande.boisson }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_index') }}">back to list</a>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}
